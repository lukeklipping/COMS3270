New files
    io.c, io.h

This iteration of the Roguelike Dungeon Generator (RLG) introduces a user interface using the ncurses library for unbuffered input and rendering. 
The PC (@) is now controlled manually using designated key commands. Automatic movement logic has been removed, but an option to toggle it on 
and off can be added if desired.


For TA:
    it works